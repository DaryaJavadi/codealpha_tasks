To run my code:

1) firstly download all needed libraries:
pip install nltk spacy scikit-learn
python -m spacy download en_core_web_sm
pip install flask

2) Then run the code:
python faq_chatbot.py

3) Go to this page, to see result in the local:
http://127.0.0.1:5000
